spring:
  application:
    name: logistica
  
  datasource:
    url: jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:5432/postgres
    username: postgres.jvxofmobzjyhfxhivxrl
    password: milocatameli
    driver-class-name: org.postgresql.Driver
    hikari:
      data-source-properties:
        assumeMinServerVersion: '9.0'
      maximum-pool-size: 10
      connection-timeout: 30000
    # Deshabilitar scripts SQL automáticos para Supabase
    initialization-mode: never
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        # dialect: org.hibernate.dialect.PostgreSQLDialect  # Auto-seleccionado por Hibernate
        format_sql: true

server:
  port: 8081

logging:
  level:
    root: INFO
    tpi_grupo46.logistica: DEBUG

# OpenAPI/Swagger Configuration
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
